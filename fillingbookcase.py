class Solution(object):
    def minHeightShelves_h (books, shelf_width, cur_shelf, cur_shelf_wid, cur_height, tot_height) {
        if books[i][0] + cur_shelf_wid > cur_shelf:
            return minHeightShelves(books, shelf_width, cur_shelf+1, 0, )
        books[i][0]
        
        
    }
    def minHeightShelves(self, books, shelf_width):
        """
        :type books: List[List[int]]
        :type shelf_width: int
        :rtype: int
        """
        min(placeOnCurrent(), placeOnNewShelf())